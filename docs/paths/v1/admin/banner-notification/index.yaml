get:
  tags:
    - Banner Notification
  summary: Get Banner Notifications
  description: Retrieve all Banner Notifications
  operationId: GetBannerNotifications
  responses:
    "200":
      description: "Successfully"
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../../../schemas/BannerNotification.yaml"
    "404":
      description: "Not found"