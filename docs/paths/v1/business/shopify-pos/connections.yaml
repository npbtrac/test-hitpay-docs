post:
  tags:
    - Shopify POS
  summary: Create Shopify POS connection
  description: Create Shopify POS connection
  operationId: CreateShopifyPOSConnection
  parameters:
    - name: business_id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      description: 'Successfully'
      content:
        application/json:
          schema:
            $ref: '../../../../schemas/IntegrationConnection.yaml'
    '404':
      description: 'Not Found'